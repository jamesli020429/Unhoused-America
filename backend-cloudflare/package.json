{
    "name": "unhoused-america-backend-cloudflare",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "d1:create": "wrangler d1 create unhoused-personas",
        "d1:init": "wrangler d1 execute unhoused-personas --file=./schema.sql",
        "d1:init-local": "wrangler d1 execute unhoused-personas --local --file=./schema.sql"
    },
    "devDependencies": {
        "wrangler": "^3.94.0"
    },
    "dependencies": {
        "@google/generative-ai": "^0.24.1",
        "axios": "^1.13.2"
    }
}